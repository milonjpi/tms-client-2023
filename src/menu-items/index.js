import dashboard from './dashboard';
import pages from './pages';
import libraries from './libraries';
import utils from './utils';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, pages, libraries, utils],
};

export default menuItems;
